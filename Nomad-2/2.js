/* 
let은 변수의 값을 바꿀 수 있지만, 
const는 바꾸지 못함(처음 초기화 한 값으로만 사용 가능)
되도록 const를 쓰는 것이 좋음
*/

//String
const str = "Hello";
console.log(str);

//Boolean
const bon = true;
console.log(bon);

//Number
const num = 100;
console.log(num);

//Float
const flt = 55.1;
console.log(flt);